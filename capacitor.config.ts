import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig={appId:'com.sakura.writingstudio',appName:'Sakura Writing Studio',webDir:'mobile-dist',android:{backgroundColor:'#071a1d',allowMixedContent:false},server:{androidScheme:'https'}};
export default config;
